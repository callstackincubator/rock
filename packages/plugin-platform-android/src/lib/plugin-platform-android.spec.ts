import { pluginPlatformAndroid } from './plugin-platform-android';

describe('pluginPlatformAndroid', () => {
  it('should work', () => {
    expect(pluginPlatformAndroid()).toEqual('plugin-platform-android');
  });
});
